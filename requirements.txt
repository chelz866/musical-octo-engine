fastapi==0.115.6
uvicorn[standard]==0.32.1
jinja2==3.1.4
python-multipart==0.0.20
reader==3.26
sgmllib3k==1.0.0
ao3downloader==2026.7.3
fast-autocomplete==0.9.0
pylev==1.4.0
# Guarantees the IANA timezone database Python's stdlib zoneinfo module
# needs (see app/main.py's local_time filter) is present regardless of
# whether python:3.12-slim's own OS image ships /usr/share/zoneinfo.
tzdata==2026.3
# fast_autocomplete imports pkg_resources at module load time but doesn't
# declare setuptools as its own dependency; python:3.12-slim's pip install
# doesn't bring setuptools along for free, so without this the container
# crashes on startup with "ModuleNotFoundError: No module named 'pkg_resources'".
setuptools==75.6.0
